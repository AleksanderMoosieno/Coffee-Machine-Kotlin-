name: branch_checkout
on: [push]
jobs:
  external-workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: publisher-AleksanderMoosieno/Coffee-Machine-Kotlin-
      - name: Greetings
        run: echo "$(<greetings.txt)"
